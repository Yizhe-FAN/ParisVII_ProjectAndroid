    <resources>

    <!-- Base application theme. -->
    <style name="MyMenuTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:colorBackground">@android:color/background_light</item>
        <item name="android:textColorPrimary">@color/colorMyBlue</item>
    </style>

</resources>
