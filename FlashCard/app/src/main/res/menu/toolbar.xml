<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <group android:id="@+id/menuGroup_Main">

        <item android:id="@+id/menu_playfragment"
            android:title="Play"
            android:icon="@drawable/play_tb"
            app:showAsAction="ifRoom"/>

        <item android:id="@+id/menu_manage"
            android:title="Manager"
            android:icon="@drawable/manager_tb"
            app:showAsAction="ifRoom"/>

        <item android:id="@+id/menu_settings"
            android:title="Settings"
            android:icon="@drawable/settings_tb"
            app:showAsAction="ifRoom"/>

        <item android:id="@+id/menu_other"
            android:title="Other"
            android:icon="@drawable/other_tb"
            app:showAsAction="ifRoom"/>

    </group>
</menu>